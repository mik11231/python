#!/usr/bin/python

execfile("1000.py")

def 

lst = [s1, u2, u3, u4, u5, u6]
lst = [s1, u2, u3, u5, u6]
tmp=u4 

def findInsertIndex(item, lst):
  # Find the index where item belongs in lst
  # return that index
#end findInsertIndex()

def insertion(lst):
  # Starting state:
    # sorted list = size 1
    #  range: 0,1
    # unsorted list = size n-1
    #  range: 1, len(lst)-1
  # Initialize x to 1
  # Grab the item at x (the first unsorted position)
  # Search sorted list for the index where lst[x] belongs
    # findInsertIndex(lst[x], lst[0:x])
    # Use list.pop() to remove the original instance of the item.
    # Use list.insert() to add/copy item into desired position.
  # Repeat 'til you're out of unsorted list.
# end insert()


